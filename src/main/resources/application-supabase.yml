# Render 실서버 환경 전용 독립 파일
spring:
  datasource:
    # 1. URL 뒤의 ?user=... 파라미터를 제거합니다.
    url: jdbc:postgresql://aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require
    # 2. 빈 값("")으로 되어 있던 username에 테넌트 ID(postgres.alyphoareyjeeizdbwqx)를 설정합니다.
    username: postgres.alyphoareyjeeizdbwqx
    password: qlalfqjsghskehahffk!
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
spring:
  # 🗄️ Supabase DB 설정
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres
    username: postgres.alyphoareyjeeizdbwqx
    password: "3m8wrYlbmOVxF5Uq"
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: HikariPool-SupabaseFinal
      minimum-idle: 1
      maximum-pool-size: 5
      connection-timeout: 30000

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

  # 🚀 수정됨: servlet 설정을 spring 하위로 이동
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  tomcat:
    connection-timeout: 60000
    threads:
      max: 20
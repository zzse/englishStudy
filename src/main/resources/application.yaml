spring:
  # 🗄️ Supabase DB 설정 (성공한 aws-1 주소 및 패스워드 완벽 세팅)
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres
    username: postgres.alyphoareyjeeizdbwqx
    password: "3m8wrYlbmOVxF5Uq"
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: HikariPool-SupabaseFinal
      minimum-idle: 1
      maximum-pool-size: 5
      connection-timeout: 30000

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false

# 스터디 대용량 파일 및 톰캣 트래픽 제어
servlet:
  multipart:
    max-file-size: 30MB
    max-request-size: 50MB
server:
  tomcat:
    connection-timeout: 60000
    threads:
      max: 20